/**
 * Simple test to verify the testing setup
 */

// This is a basic test to make sure our testing environment works
describe('Basic test', () => {
  it('should pass a simple assertion', () => {
    expect(1 + 1).toBe(2);
  });
});
